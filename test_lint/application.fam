# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="bad_lint",  # Must be unique
    name="App bad_lint",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="bad_lint_app",
    stack_size=2 * 1024,
    fap_category="Misc",
    # Optional values
    # fap_version=(0, 1),  # (major, minor)
    fap_icon="bad_lint.png",  # 10x10 1-bit PNG
    # fap_description="A simple app",
    # fap_author="J. Doe",
    # fap_weburl="https://github.com/user/bad_lint",
    fap_icon_assets="images",  # Image assets to compile for this application
)
